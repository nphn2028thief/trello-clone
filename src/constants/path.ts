export enum EPath {
  HOME = "/",
  SIGNIN = "/sign-in",
  SIGNUP = "/sign-up",
  SELECT_ORGANIZATION = "/select-org",
  ORGANIZATION = "/organization",
  ORGANIZATION_DETAIL = "/organization/:id",
}
